# Servidor
server.port=${PORT:8003}

# Base de datos
spring.datasource.url=${DATABASE_URL}
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# JWT - misma clave que el Auth Service
jwt.secret=${SECRET_KEY}

# URLs de servicios externos
auth.service.url=${AUTH_SERVICE_URL:http://auth-service:8001}
space.service.url=${SPACE_SERVICE_URL:http://space-service:8002}
billing.service.url=${BILLING_SERVICE_URL:http://billing-service:8004}

spring.application.name=reservation-service

# Incluir mensaje detallado en respuestas de error
server.error.include-message=always
